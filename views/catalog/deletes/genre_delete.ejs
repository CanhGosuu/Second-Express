<!DOCTYPE html>
<html lang="en">

<head>
    <% include ../../layouts/head %>
</head>

<body class="container">
    <!-- header -->
    <% include ../../layouts/header %>
    <!-- main -->
    <h1>
        <%=title%>:
        <%=genre.name%>
    </h1>
    <br><br>
    <%if(genre_books.length){ %>
    <p><strong>Delete the following books before attempting to delete this genre.</strong></p>
    <div style="margin-left:20px;margin-top:20px">
        <h4>Books</h4>
        <dl>
            <% for(var i=0; i<genre_books.length; i++) {%>
            <dt> <a href="<%=genre_books[i].url%>">
                    <%=genre_books[i].title%>
                </a>
            </dt>
            <dd>
                <%=genre_books[i].summary%>
            </dd>
            <% } %>
        </dl>
    </div>
    <% } else{ %>
    <p>Do you really want to delete this Genre?</p>

    <form action="" method="post">
        <div class="form-group">
            <input type="text" hidden required name="genreid" value="<%=genre._id%>" class="form-control">
        </div>
        <button type="submit" class="btn btn-primary">Delete</button>
    </form>
    <% } %>

    <!-- main -->
    </div>
    </div>
    </div>
    <footer>
        <% include ../../layouts/footer %>
    </footer>

</body>